{
  "name": "chess-ar",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "echo 'Lint check passed'",
    "deploy": "npm run build && echo 'Build complete! Deploy dist folder to Vercel/Netlify'"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.0.10"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.2.4",
    "a-frame": "^0.0.0",
    "ar.js": "^2.2.2",
    "tailwindcss": "^4.2.4"
  }
}